# volleyball-stats

```
$ npm create vite@latest volleyball-stats -- --template react-ts
$ npm install @mui/material @emotion/react @emotion/styled
$ npm install @mui/icons-material
$ npm install --save-dev gh-pages
```